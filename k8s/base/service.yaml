apiVersion: v1
kind: Service
metadata:
  name: credit-risk-api
  namespace: credit-risk
  labels:
    app: credit-risk-api
spec:
  type: ClusterIP
  selector:
    app: credit-risk-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
