apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: credit-risk-api
  namespace: credit-risk
spec:
  selector:
    matchLabels:
      app: credit-risk-api
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
